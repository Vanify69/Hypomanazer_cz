{
  "name": "hypomanager-bank-autofill",
  "version": "0.1.0",
  "description": "Bezpečné vyplňování bankovních formulářů z aktivního případu v HypoManageru.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && vite build && node scripts/copy-assets.js",
    "dev": "tsc && vite build && node scripts/copy-assets.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.3.0",
    "vite": "^5.4.0"
  }
}
